import {ParcelStatusHistory} from './parcel-status-history';

describe('ParcelStatusHistory', () => {
  it('should create an instance', () => {
    expect(new ParcelStatusHistory()).toBeTruthy();
  });
});
