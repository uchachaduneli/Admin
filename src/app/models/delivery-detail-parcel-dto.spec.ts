import { DeliveryDetailParcelDTO } from './delivery-detail-parcel-dto';

describe('DeliveryDetailParcelDTO', () => {
  it('should create an instance', () => {
    expect(new DeliveryDetailParcelDTO()).toBeTruthy();
  });
});
